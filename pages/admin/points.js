import Layout from '../../components/Layout';
import { Typography } from '@mui/material';

const Points = () => {
  return (
    <Layout>
      <Typography variant="h4" gutterBottom>
        Points
      </Typography>
      <Typography>
        Gérer les points des utilisateurs ici.
      </Typography>
    </Layout>
  );
};

export default Points;
