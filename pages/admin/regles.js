import Layout from '../../components/Layout';
import { Typography } from '@mui/material';

const Regles = () => {
  return (
    <Layout>
      <Typography variant="h4" gutterBottom>
        Règles
      </Typography>
      {/* Ajouter ici votre logique pour les règles */}
    </Layout>
  );
};

export default Regles;
